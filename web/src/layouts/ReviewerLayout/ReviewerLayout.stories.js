import ReviewerLayout from './ReviewerLayout'

export const generated = (args) => {
  return <ReviewerLayout {...args} />
}

export default {
  title: 'Layouts/ReviewerLayout',
  component: ReviewerLayout,
}
